<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>index</title>
    </head>
    <body>
        <h2>Recipes</h2>
        <% @recipes.each do |recipe| %>
            <p><a href='/recipes/<%=recipe.id%>'><%= recipe.name %></a></p>
        <% end %>
    </body>
</html>